# See https://www.dartlang.org/guides/libraries/private-files

# Files and directories created by pub
.dart_tool/
.packages
build/
pubspec.lock

*.iml

# custom_lint debug log (created when `custom_lint: debug: true` is set)
custom_lint.log

# Disposable scratch file written by test/expect_lint_test.dart to exercise
# --fix without mutating a committed fixture; the test deletes it itself.
lib/_add_await_fix_scratch.dart

pubspec_overrides.yaml
