# Методичка к докладу «Binding, Module, Scope»

Инструкция для спикера к файлу `cherrypick-presentation-conference.md`.

---

## 1. Жанр и тональность

**Жанр:** инженерный рассказ «Я думал над этой задачей и вот что сделал».

Не сравнительный бенчмарк — первые три слайда дают контекст пространства решений,
а не рейтинг. Не рекламная питч-дека — библиотека не представляется как лучшая.
Не туториал — аудитория должна уйти с моделью, а не с выученными методами.

**Тональность:** ровная инженерная. Без самоуничижения («моя библиотека маленькая,
но…») и без гиперболы («единственное правильное решение»).

**Центральная мысль, которую слушатель должен унести:**
> Скоуп — это не папка для сервисов, а группа объектов с общей судьбой.
> Закрыли скоуп — ушли все.

Если эта мысль прошла — доклад удался, независимо от того, возьмут ли CherryPick.

---

## 2. Форматы

Маркеры `✂` в файле делят слайды на три пула. Готовьте конкретный формат заранее:
PDF-экспорт с нужными слайдами удобнее, чем пролистывание мимо нерелевантных.

### 30 минут — слайды 1–17

| Что включено | Время |
|---|---|
| Контекст: карта подходов | 3 мин |
| Ядро CherryPick | 23 мин |
| «Куда дальше» + Q&A | 4 мин |

Подходит для конференций с жёстким тайм-слотом и широкой аудиторией.
Цель: дать модель, не перегрузить деталями.

### 45 минут — слайды 1–17 + Р1–Р8

| Что добавляется | Время |
|---|---|
| Именованные биндинги | 1 мин |
| Параметры выполнения | 1,5 мин |
| Асинхронные зависимости | 1,5 мин |
| Когда зависимости может не быть | 1 мин |
| Кодогенерация | 2 мин |
| Циклические зависимости | 1,5 мин |
| Наблюдатель | 1,5 мин |
| Кто закрывает скоуп экрана | 2 мин |

Расширение показывается **после ядра**, перед «Куда дальше».
Подходит, когда аудитория уже знакома с DI в целом и хочет API-детали.

### 60 минут — всё выше + Г1–Г3

| Что добавляется | Время |
|---|---|
| Граф из пакетов (монорепо) | 1,5 мин |
| Аннотации кодогенератора | 1,5 мин |
| Цена выбора | 1,5 мин |
| Q&A (с резервными слайдами) | ~10 мин |

Глубокое погружение добавляется **после расширения**.
Подходит для митапа с узкой Flutter-аудиторией, которая уже пробовала DI.

---

## 3. Хронометраж по слайдам

Ориентир для 30-минутного формата.

| # | Слайд | Хронометраж | Мин |
|---|---|---|---|
| 1 | Binding, Module, Scope | 0:00–0:01 | 1 |
| 2 | Два вопроса | 0:01–0:03 | 2 |
| 3 | Выбор: дерево скоупов | 0:03–0:04 | 1 |
| 4 | Три понятия | 0:04–0:06 | 1,5 |
| 5 | Рабочий код | 0:06–0:07 | 1,5 |
| 6 | Виды биндингов | 0:07–0:09 | 2 |
| 7 | Зачем дерево | 0:09–0:11 | 2 |
| 8 | Как открыть и закрыть | 0:11–0:13 | 2 |
| 9 | Ребёнок видит родителя | 0:13–0:14 | 1,5 |
| 10 | Disposable | 0:14–0:16 | 2 |
| 11 | Модуль — это объект | 0:16–0:18 | 1,5 |
| 12 | Подмена в тестах | 0:18–0:20 | 2 |
| 13 | Flutter: доступ | 0:20–0:21 | 1,5 |
| 14 | Три рецепта | 0:21–0:23 | 2 |
| 15 | Чего не делать | 0:23–0:25 | 2 |
| 16 | Где это проверено | 0:25–0:26 | 1 |
| 17 | Куда дальше | 0:26–0:27 | 1 |
| — | Q&A | 0:27–0:30 | 3 |

Если ощущаете, что отстаёте, сокращайте слайды 11 («Модуль») и 16 («Где проверено»):
первый хорошо понятен из кода, второй — факты на память. Никогда не сокращайте
слайды 4, 7, 9 — это несущая конструкция доклада.

---

## 4. Пять ключевых моментов

### 4.1 Пауза на первом слайде

Подзаголовок «Больше в CherryPick DI знать нечего» — это провокация, не факт.
Сделайте паузу после него в 2–3 секунды. Аудитория должна поднять бровь
или усмехнуться. Если реакции нет — скажите вслух: «Спойлер: кое-что ещё знать
придётся, но удивительно мало».

### 4.2 Слайд «Два вопроса» — карту показывают, не читают

Этот слайд объясняет **пространство**, а не оценивает игроков.
Покажите быстро: «Вот два вопроса, которые всё определяют. Ось одна — кто
решает когда умирать. Ось вторая — когда узнаёте об ошибке. CherryPick
вот здесь. Дальше объясню почему.» — и переходите. Не застревайте в объяснении
каждой позиции.

### 4.3 Слайд «Выбор: дерево скоупов» — читайте медленно

Три абзаца: задача, ограничение, решение. Это единственный слайд, где объясняется
**зачем** библиотека существует. Именно здесь называется цена — рантайм-резолвинг.
Назовите её нейтрально: «Я намеренно пошёл на это. Платим отсутствием ошибок
компилятора, получаем низкий барьер входа.»

### 4.4 Слайд «Зачем дерево» — пауза на диаграмме

Дерево скоупов — самый фотографируемый слайд. Дайте 10–15 секунд просто
посмотреть. Потом произнесите ключевую фразу: «Закрыли `session` — ушло всё
поддерево. Один вызов. Не нужно помнить список.»

### 4.5 Слайд «Три рецепта» — это момент «я могу это применить сегодня»

Логаут в одну строку — самый часто цитируемый кусок кода после докладов. Прочитайте
`await CherryPick.closeScope(scopeName: 'session')` вслух. Если успеваете —
спросите зал: «У кого логаут сейчас реализован так же просто?»

---

## 5. Ловушки API в слайдах

Знайте наизусть — вопросы по ним возникают сразу после показа кода.

### Ловушка 1: `toProvide` без `.singleton()` — не кешируется

Каждый `resolve` создаёт **новый экземпляр**. Слайд «Рабочий код» намеренно
показывает `.singleton()`, потому что для `ApiClient` это ожидаемое поведение.
Без него два разных места кода получат два разных клиента.

```dart
// ОК — один ApiClient на скоуп
bind<ApiClient>().toProvide(() => ApiClient()).singleton();

// ЛОВУШКА — каждый resolve = новый объект
bind<ApiClient>().toProvide(() => ApiClient());
```

### Ловушка 2: `SilentCherryPickObserver` — нельзя наследовать

```dart
// ЛОВУШКА — наблюдатель замолчит навсегда
class MyObserver extends SilentCherryPickObserver { ... }

// ВЕРНО
class MyObserver implements CherryPickObserver { ... }
```

Scope включает fast-path по `is SilentCherryPickObserver`. Наследник попадает
в него и не получает ни одного колбэка. Только `implements`.

### Ловушка 3: `toProvideAsync` — строго `Future<T>`

```dart
// ЛОВУШКА — не компилируется в toProvideAsync
bind<Db>().toProvideAsync(() => computeSyncDb());   // FutureOr<T>

// ВЕРНО — явный Future
bind<Db>().toProvideAsync(() async => Database.open('app.db'));
```

`toProvide` принимает `FutureOr<T>`. `toProvideAsync` — строго `Future<T>`.
Для асинхронных зависимостей всегда используйте `toProvideAsync`.

### Ловушка 4: `tryResolve` — null только при отсутствии биндинга

```dart
// tryResolve вернёт null ТОЛЬКО если биндинг не зарегистрирован
final a = scope.tryResolve<Analytics>();   // null — биндинга нет, всё ОК

// Незавершённый биндинг — это всё равно StateError, не null
```

`tryResolve` не «глотает» все ошибки. Зарегистрированный биндинг, упавший
при создании, бросит `StateError`.

### Ловушка 5: `.singleton()` на параметрическом биндинге

```dart
// ЛОВУШКА — кешируется первый параметр
bind<OrderRepo>().toProvideWithParams(
  (params) => OrderRepo(id: params as String, ...),
).singleton();

// первый вызов: resolve(params: 'order-42') → кеш OrderRepo(id: 'order-42')
// второй вызов: resolve(params: 'order-99') → вернёт тот же OrderRepo('order-42')
```

### Ловушка 6: `TestOverridesModule` должен идти после боевых

Побеждает последний биндинг одного типа. Порядок в `installModules` важен:

```dart
// ВЕРНО — тест перекрывает боевой
..installModules([AppModule(), TestOverridesModule()]);

// ЛОВУШКА — боевой перекрывает тест
..installModules([TestOverridesModule(), AppModule()]);
```

### Ловушка 7: `closeRootScope()` в tearDown обязателен

```dart
tearDown(() => CherryPick.closeRootScope());
```

Без этого тесты текут друг в друга через глобальное дерево. Паттерн «не закрыл
в tearDown → следующий тест видит объекты предыдущего» встречается стабильно
в первые дни работы с библиотекой.

### Ловушка 8: `openScope`/`closeScope` — `@experimental`

На слайде «Как открыть и закрыть» показаны оба стиля. `openScope` и `closeScope`
по пути `scopeName` помечены `@experimental`. Анализатор даёт три предупреждения
`experimental_member_use` — это норма для dev.4, не ошибки.

### Ловушка 9: именованные биндинги — неочевидный порядок

```dart
// Оба варианта эквивалентны (порядок withName/toProvide свободный)
bind<ApiClient>().withName('prod').toProvide(() => ApiClient('api.example.com'));
bind<ApiClient>().toProvide(() => ApiClient('api.example.com')).withName('prod');
```

Аудитория часто спрашивает, можно ли `withName` после `toProvide`. Можно.

### Ловушка 10: запутанная номенклатура детекторов циклов

| Метод | Что реально делает |
|---|---|
| `scope.enableCycleDetection()` | Локальный детект (внутри скоупа) |
| `scope.enableGlobalCycleDetection()` | **Кросс-скоуп** детект (через границы) |
| `CherryPick.enableGlobalCycleDetection()` | Локальный для **всех** скоупов |
| `CherryPick.enableGlobalCrossScopeCycleDetection()` | Кросс-скоуп для **всех** |

Называйте вслух: «`enableGlobalCycleDetection` на уровне CherryPick — локальный
для всех. Название запутано исторически.»

---

## 6. Типы аудитории

### Новичок в DI

Не знает, зачем DI вообще нужен. Задержитесь на слайде «Выбор: дерево скоупов»,
объясните устно задачу: «Представьте приложение с логином. Пока пользователь
вошёл — одни объекты. Вышел — они должны уйти. Кто это организует?»
Слайды 4–9 воспринимаются ими легче всего.

### Пользователь get_it

Знает DI, привык к `GetIt.I.get<T>()`. Его вопрос: «Зачем переусложнять?»
Ответ прямо в слайде «Зачем дерево»: два заказа одновременно в линейном стеке
невозможны без хаков. Скоуп — это не усложнение, это способ выразить
параллельные ветки жизни.

### Пользователь riverpod

Знает состояния и провайдеры. Для него ключевая разница: в riverpod время жизни
определяется деревом виджетов или явным `.autoDispose`. В CherryPick — явным
деревом скоупов, не связанным с виджетами. Это позволяет управлять жизнью
бизнес-объектов независимо от UI.

### Архитектор монорепозитория

Интересуется организацией в масштабе. Слайды «Модуль — это объект» и «Граф из
пакетов» (расширение) — его слайды. Ключевая мысль: пакет отдаёт `Module`, не знает
о приложении. Состав фичи виден одним объектом.

### Скептик

Начнёт с «Почему не использовать X?». Не спорьте — признайте сразу:
«Это нишевое решение. Если вас устраивают гарантии компилятора — возьмите yx_scope.
Если хотите низкий барьер входа и гибкость времени жизни — посмотрите на это.»
Слайд «Цена выбора» (расширение) специально для него.

---

## 7. Q&A: подготовленные ответы

### «Почему рантайм, а не компиляция?»

«Сознательный обмен: рантайм проще для освоения, ниже барьер входа.
Платим тем, что забытый биндинг даёт ошибку при запуске, не при компиляции.
На практике это покрывается тестами и детектором циклов в debug-сборке.»

### «Один мейнтейнер — это не страшно?»

«Apache 2.0 — форкается без разрешения. Если проект бросить, библиотека
остаётся в своём последнем состоянии и продолжает работать. Зависимость
от одного мейнтейнера — честный риск, который стоит учитывать при выборе.»

### «Чем отличаетесь от yx_scope?»

Достаньте резервный слайд Р1. Коротко: «Оба — дерево скоупов. yx_scope даёт
ошибки при компиляции через типизированные поля, CherryPick — рантайм-резолвинг
с меньшим числом концепций. Разные обмены, одна задача.»

### «У get_it тоже есть scopes, зачем переходить?»

«В get_it scope — это стек: открыл, поработал, закрыл. Два scope одновременно
на одном уровне дерева (два активных заказа, два аккаунта) — это хак через
два отдельных GetIt-инстанса. В CherryPick сиблинги — первоклассный сценарий.»

### «Насколько быстро?»

Достаньте резервный слайд «Производительность»: «5,6 мс на 1000 биндингов в AOT —
меньше 1% от 608 мс до первого кадра. Бенчмарк в репо, повторяется.»

### «Можно ли мигрировать с get_it не всё сразу?»

Достаньте резервный слайд «Постепенная миграция». Коротко: «Оберните старый
`GetIt.I.get<T>()` в биндинг-мост. Новые фичи — на скоупах. Мигрируйте слой
за слоем. Каждый шаг самодостаточен.»

### «Какая версия стабильна?»

«4.0.0-dev.4 — предрелиз, который мы используем в проде. `dev` здесь —
версионная метка, а не признак нестабильности: API устоялось, breaking-изменений
до 4.0.0 не планируется. Если важен стабильный тег — ждите 4.0.0.»

### «Как отлаживать, если что-то не резолвится?»

«Три инструмента: `CherryPick.getCurrentResolutionChain()` покажет,
где цепочка обрывается; наблюдатель (`CherryPickObserver`) — все события;
`enableCycleDetection()` в debug-сборке — циклы.»

---

## 8. Что заменить перед показом

| Маркер | Где | Что подставить |
|---|---|---|
| `[ЗАМЕНИТЬ: событие, дата]` | Слайд 1 | Название конференции и дата |
| `[ЗАМЕНИТЬ: контакт + QR-код]` | «Куда дальше» | Telegram / email + QR |
| `[ЗАМЕНИТЬ: перерисуйте схему графикой]` | «Два вопроса», «Зачем дерево» | Нормальная графика |

Два слайда-схемы (карта подходов и дерево скоупов) **фотографируют** на конференциях.
ASCII-art в PDF не читается с 5 метров. Перерисуйте как приоритет номер один.

---

## 9. Структура Q&A

Оставьте «Куда дальше» на экране весь Q&A.

Если вопросов нет 30 секунд, предложите сами:
«Что было непонятно?» работает лучше, чем «Есть вопросы?»

Если вопрос уходит в детали конкретного кейса — предложите разобраться после:
«Давайте возьмём ваш случай отдельно — вот контакт.» Не застревайте в одном
вопросе дольше 3 минут.

Резервные слайды держите отдельно (или в конце PDF). Не показывайте их в основном
потоке — только по запросу.

---

## 10. Чеклист перед докладом

**За неделю**
- [ ] Выбрать формат (30 / 45 / 60 мин) и подготовить PDF с нужными слайдами
- [ ] Перерисовать схемы на слайдах «Два вопроса» и «Зачем дерево»
- [ ] Заполнить `[ЗАМЕНИТЬ: событие, дата]` и контакт на последнем слайде

**За день**
- [ ] Прогнать доклад целиком с таймером (не пропуская слайды)
- [ ] Проверить, что `dart analyze` на код слайдов даёт ноль ошибок
- [ ] Подготовить резервные слайды отдельным PDF или в конце основного

**За час**
- [ ] Открыть PDF на слайде 1, убедиться, что фонт читается с 5 метров
- [ ] Проверить, что резервные слайды доступны быстро (Ctrl+G по номеру страницы)
- [ ] Убедиться, что «Куда дальше» — последний слайд, который увидит зал

---

## Приложение: хронометраж расширения

Для 45-минутного формата расширение Р1–Р8 идёт после слайда 16 («Куда дальше»
остаётся финальным).

| # | Слайд | Мин |
|---|---|---|
| Р1 | Именованные биндинги | 1 |
| Р2 | Параметры выполнения | 1,5 |
| Р3 | Асинхронные зависимости | 1,5 |
| Р4 | Когда зависимости может не быть | 1 |
| Р5 | Кодогенерация | 2 |
| Р6 | Циклические зависимости | 1,5 |
| Р7 | Наблюдатель | 1,5 |
| Р8 | Кто закрывает скоуп экрана | 2 |
| — | Q&A + «Куда дальше» | ~3 |
| **Итого** | | **~15 мин** |

## Приложение: вступительный крючок

Первые 30–60 секунд речи. Три варианта с разным характером входа — выбирайте
под ощущение от зала.

### Вариант 1 — через боль аудитории

*Работает на любой аудитории. Поднятые руки создают контакт с залом.*

> «Поднимите руку, у кого логаут когда-нибудь протекал.
> То есть — нажали выйти, а потом обнаружили, что объекты
> прошлой сессии где-то всё ещё живут.
>
> *(Пауза. Смотрите на руки.)*
>
> Хорошо. Держите руки — ещё один вопрос.
> У кого это случилось больше одного раза?
>
> *(Пауза.)*
>
> Вот именно. Сегодня я расскажу, почему это повторяется —
> и покажу, как логаут становится одной строкой кода.»

### Вариант 2 — через провокацию заголовка

*Работает, если хочется сразу взять тон "уверенный, но самоироничный". Пауза обязательна.*

> «Binding, Module, Scope. Больше знать нечего.
>
> *(Пауза 3 секунды. Дайте залу прочитать и отреагировать.)*
>
> Ну, почти. Кое-что знать ещё придётся — но удивительно мало.
>
> За следующие тридцать минут я покажу три понятия,
> после которых логаут — это одна строка,
> два аккаунта одновременно — не архитектурная проблема,
> и тесты перестают течь друг в друга.
>
> Начнём с того, почему вообще нужен ещё один DI-фреймворк.»

### Вариант 3 — через парадокс

*Работает на технически сильной аудитории. Интеллектуальный вход без вопросов к залу.*

> «DI обычно обсуждают через создание.
> Как создать объект. Как передать зависимость.
>
> У меня был другой вопрос: кто решает, когда объект умрёт?
>
> *(Пауза.)*
>
> В большинстве приложений ответ — никто конкретный.
> Процесс завершился — всё ушло.
> Или объект живёт, пока виджет на экране.
> Или вы вручную вызываете unregister и надеетесь, что не забыли.
>
> Я захотел, чтобы на этот вопрос был чёткий структурный ответ.
> Вот что из этого вышло.»

### Как выбрать

| Ситуация | Вариант |
|---|---|
| Незнакомый зал, разношёрстная аудитория | 1 — руки вверх снимают напряжение |
| Зал уже «тёплый», есть атмосфера | 2 — игра с заголовком |
| Митап с сильными разработчиками, академичный тон | 3 — парадокс |

---

## Приложение: хронометраж глубокого погружения

Для 60-минутного формата после расширения.

| # | Слайд | Мин |
|---|---|---|
| Г1 | Граф из пакетов | 1,5 |
| Г2 | Аннотации кодогенератора | 1,5 |
| Г3 | Цена выбора | 1,5 |
| — | Q&A + «Куда дальше» | ~10 |

---

## Приложение: текст по слайдам

Заготовка речи для каждого слайда. Не скрипт для чтения вслух — основа,
которую адаптируете под свой темп и реакцию зала.

---

### Слайд 1 · Binding, Module, Scope · ~1 мин

Три слова. Это весь словарь CherryPick DI. Binding, Module, Scope.

«Больше знать нечего» — ну, почти. Кое-что знать ещё придётся, но удивительно
мало. И это было сознательным решением при проектировании: я хотел, чтобы
новый разработчик в команде мог начать писать код на следующий день после
знакомства с библиотекой.

Я Сергей, Flutter-разработчик, автор CherryPick DI. Сегодня покажу, что
скрывается за этими тремя словами — и почему именно такой набор.

---

### Слайд 2 · Два вопроса · ~2 мин

Прежде чем перейти к CherryPick — покажу карту. Не чтобы сравнивать, а чтобы
было понятно, о каком пространстве решений мы вообще говорим.

DI-фреймворки для Dart и Flutter различаются по двум осям.

Первая: чем задано время жизни объекта? Самый простой ответ — ничем: живёт
пока живёт процесс. Так работают глобальные синглтоны. Дальше — привязка
к дереву виджетов, как в provider. Подписки, как в riverpod. Стек с командами
push и pop, как в get_it. И наконец — явное дерево скоупов, которое строите вы:
yx_scope, CherryPick.

Вторая ось: когда узнаёте, что зависимость не зарегистрирована? Yx_scope
ответит при компиляции. Кодогенераторы — при генерации. Get_it и CherryPick —
при запуске. У пользователя — если не писать тесты.

Это не рейтинг. В каждой клетке — рабочие решения для конкретных задач.

CherryPick вот здесь: дерево скоупов, рантайм-резолвинг. Сейчас объясню почему.

---

### Слайд 3 · Выбор: дерево скоупов · ~1 мин

Когда я проектировал CherryPick, у меня была конкретная задача.

В нашем приложении нужны параллельные ветки жизни — два активных заказа
одновременно, два аккаунта, фоновые процессы. Линейный стек с этим справляется
плохо: два активных заказа в стеке — это два отдельных контейнера. Привязка
к виджетам — не вариант: бизнес-объекты не должны зависеть от того, какой
экран открыт.

И ещё одно ограничение: команда должна освоить новый подход за один день.

Отсюда — рантайм-резолвинг. Да, забытый биндинг даст ошибку при запуске,
а не при компиляции. Это цена, и я называю её сразу. В обмен — низкий барьер
входа и ровно три понятия.

---

### Слайд 4 · Три понятия, и это всё · ~1,5 мин

Binding — правило создания. Не сам объект, а инструкция: вот как создать
объект типа T.

Module — набор биндингов, оформленный как объект. Можно передать параметром,
выбрать по условию, положить в список.

Scope — узел дерева. Живёт, пока его явно не закроете. Держит свои объекты.
Закрыли — ушло всё.

Вот и всё. Всё, что увидите дальше — уточнения к этим трём.
Именованные биндинги? Уточнение к Binding. Кодогенерация? Другой способ
написать Binding. Дерево скоупов? Это про Scope.

Держите три слова в голове — к ним будет незримо возвращаться весь доклад.

---

### Слайд 5 · Рабочий код · ~1,5 мин

Минимальный рабочий пример. Реальный код — компилируется.

AppModule наследует Module. В builder объявляем биндинг: bind<ApiClient> через
toProvide — это фабрика — плюс singleton, один экземпляр на скоуп.

В main: открываем корневой скоуп, устанавливаем модуль, резолвим ApiClient.
Три шага: объявить модуль, поставить в скоуп, попросить объект.

Каскад `..installModules` — не ошибка. installModules возвращает сам Scope,
цепочка читается как одно выражение.

Обратите внимание на `.singleton()` — без него каждый resolve создаст новый
ApiClient. Про это — следующий слайд.

---

### Слайд 6 · Виды биндингов · ~2 мин

Три вида биндинга. Главный столбец — «живёт в скоупе».

toInstance — готовый объект. Создаётся при установке модуля, живёт в скоупе.

toProvide — фабрика. Объект создаётся при первом resolve. Без singleton —
каждый resolve даёт новый экземпляр, в скоупе объект не живёт.

toProvide плюс singleton — первый resolve создаёт и кеширует объект.
Живёт в скоупе до закрытия.

Ключевой вывод: когда скоуп закрывается, он чистит только то, что в нём живёт.
Несинглтонный toProvide — не живёт. Его уберёт GC, когда никто не держит ссылку.

И самая частая ошибка в первые недели: два разных репозитория вместо одного,
потому что забыли `.singleton()`.

---

### Слайд 7 · Зачем дерево · ~2 мин

Вот дерево скоупов из реального приложения.

Root — всё время работы. ApiClient, аналитика.

Session — от логина до логаута. Profile, Wallet.

Под session — order-7 и order-9. Два активных заказа, которые живут одновременно
и не знают друг о друге.

Отдельная ветка — nonAuth — для экрана входа.

*(Пауза.)*

Пользователь нажал logout. Что происходит?

Один вызов: closeSubScope('session'). Всё поддерево уходит вместе с ним —
Profile, Wallet, оба заказа. Не нужно помнить список. Не нужно обходить объекты
вручную.

Скоуп — это не папка для сервисов. Это группа объектов с общей судьбой.

---

### Слайд 8 · Как открыть и закрыть · ~2 мин

Два стиля работы со скоупами.

По ссылке: открыли root, из него session, из session — order-7. Закрыли session —
ушло поддерево. Используйте, когда ссылка на скоуп рядом.

По пути, строкой: удобно, когда скоуп открывался в одном месте приложения,
а закрывать нужно в другом. Не нужно тащить ссылку через весь стек вызовов.

Важная деталь: openScope и openSubScope идемпотентны. Вызвали с тем же именем —
получили существующий скоуп, новый не создался. На это можно опираться:
открывайте без проверки «а вдруг уже есть».

Методы openScope и closeScope по пути помечены @experimental. Анализатор
даёт три предупреждения experimental_member_use — единственное замечание
на весь код этой колоды.

---

### Слайд 9 · Ребёнок видит родителя · ~1,5 мин

SessionModule создаёт Wallet и передаёт в него ApiClient. Но ApiClient объявлен
в root, а не в session.

Резолв идёт вверх по дереву. Не нашли биндинг в своём скоупе — спрашиваем
родителя. Не нашли у родителя — деда. До корня.

Обратно — нет. Из root нельзя резолвить то, что объявлено в session.

Это работает как гравитация — встроено в структуру, нарушить невозможно,
не нужно помнить.

Практический вывод: долгоживущий объект из root не может зависеть от
короткоживущего из session. Если такая зависимость нужна — передавайте
аргументом метода, не через DI.

---

### Слайд 10 · Disposable · ~2 мин

Когда скоуп закрывается, GC в конечном счёте освободит память. Но GC не знает,
когда отменить StreamSubscription или закрыть сокет.

Для этого — интерфейс Disposable. Реализуйте его в любом объекте, который
держит внешний ресурс.

closeSubScope рекурсивно обходит поддерево и вызывает dispose() на каждом
disposable-объекте. Ждёт завершения — метод асинхронный.

Важная точность: скоуп dispose-ит только объекты своих биндингов. Объект,
зарезолвленный через родительский скоуп — дело родителя.

Для тех, кто переходит с get_it: там onDispose, не dispose. Это первое место,
где спотыкаются при миграции.

---

### Слайд 11 · Модуль — это объект · ~1,5 мин

В чём принципиальная разница между Module и функцией регистрации?

Функцию нельзя передать параметром, выбрать по условию в рантайме, положить
в список, собранный динамически.

Модуль — можно. NetworkModule принимает baseUrl в конструкторе. Дальше —
условный выбор: useMocks или нет. И spread из списка фичевых пакетов — каждый
пакет отдаёт свой Module.

В монорепозитории из 67 пакетов приложение само несёт около трёх процентов
биндингов. Остальное приносят фичевые пакеты.

И ещё: dropModules снимает модули со скоупа без его закрытия. Плюс installModules
— и вы переключили реализацию на живом приложении без перезапуска.

---

### Слайд 12 · Подмена в тестах · ~2 мин

Подмена — тот же механизм, что в проде. Второго API нет.

TestOverridesModule регистрирует MockApiClient. Ставим его после AppModule —
последний биндинг побеждает. Порядок в installModules важен.

В setUp открываем корневой скоуп с обоими модулями. В tearDown — обязательно
закрываем. Без этого тесты текут друг в друга через глобальное дерево.
Это самая частая ошибка в первые дни.

Преимущество: тест использует ту же механику, что прод. Хотите протестировать
отдельный модуль — откройте скоуп только с ним.

---

### Слайд 13 · Flutter: доступ из дерева виджетов · ~1,5 мин

Для интеграции с Flutter — пакет cherrypick_flutter.

В main оборачиваем приложение в CherryPickProvider. В виджете —
CherryPickProvider.of(context) даёт доступ к дереву скоупов.

Важно понять, чем CherryPickProvider не является. Он не владеет скоупами
и не закрывает их. Это точка доступа — InheritedWidget, который делает
дерево скоупов видимым из дерева виджетов.

Управление временем жизни скоупов — ваша ответственность. Кто конкретно
открывает и закрывает скоуп экрана — разбираем в расширенной части.
Коротко: либо State.dispose в StatefulWidget, либо guard роутера.

---

### Слайд 14 · Три рецепта · ~2 мин

Три сценария из практики. Покрывают большинство вопросов в первый месяц.

Логаут: одна строка. await CherryPick.closeScope с именем 'session'. Всё
поддерево ушло. Подписки отменены через Disposable. Объекты недоступны.

Два аккаунта одновременно: два сабскоупа на одном уровне — session-a
и session-b. Они независимы по определению дерева. Это не хак.

Экран-процесс: скоуп открывается на входе в многоэкранный поток — оплата,
подписание документа. Закрывается на выходе. Все промежуточные экраны
работают с одним и тем же скоупом.

В одном реальном приложении — 29 таких процессных скоупов под одним сессионным.

---

### Слайд 15 · Чего не делать · ~2 мин

Пять реальных граблей.

Первое: не кладите всё в корневой скоуп. Дерево вырождается в глобальный
контейнер, логаут снова протекает. Правило для код-ревью: «почему этот
объект в root?»

Второе: не резолвите в конструкторе модуля — только внутри builder. Иначе
объект создаётся при установке модуля, а не при первом запросе.

Третье: не держите ссылку на закрытый скоуп. После closeSubScope он непригоден.
Берите по имени — openScope идемпотентен.

Четвёртое: не ставьте .singleton() на параметрический биндинг. Закешируется
первый параметр, все последующие вызовы с другими параметрами вернут тот же объект.

Пятое: не наследуйте SilentCherryPickObserver — только implements. Иначе
наблюдатель замолчит навсегда.

---

### Слайд 16 · Где это проверено · ~1 мин

Коротко о масштабе, чтобы это не оставалось теорией.

67 пакетов, 39 скоупов, глубина дерева три уровня, 29 процессных скоупов
под одним сессионным. Логаут перестал быть местом для багов. Состав фичи
виден одним объектом-модулем.

По производительности: 5,6 миллисекунды на сборку тысячи биндингов из
43 модулей в AOT — меньше процента от 608 миллисекунд до первого кадра.

---

### Слайд 17 · Куда дальше · ~1 мин

*(Оставьте слайд на экране весь Q&A.)*

Документация — cherrypick-di.dev.

Пример приложения в репозитории: examples/market_pulse. Начинать с lib/core/di/.

Миграция с 3.x — doc/migration_4.0.0_ru.md. Четыре механических изменения.

И главное: если что-то не получилось — открывайте issue. Особенно если
не получилось. Именно такие кейсы интереснее всего.

---

## Расширение: текст по слайдам Р1–Р8

---

### Р1 · Именованные биндинги · ~1 мин

Что делать, если нужны две реализации одного интерфейса в одном скоупе?
Например, два Dio — для авторизованных и неавторизованных запросов.

Именованный биндинг: withName задаёт ключ, resolve принимает named: с тем же ключом.

Порядок withName и toProvide свободный — можно ставить в любом порядке.

---

### Р2 · Параметры времени выполнения · ~1,5 мин

Иногда объект нельзя создать без аргумента, который известен только в рантайме:
id заказа, номер счёта, идентификатор сессии.

toProvideWithParams принимает функцию с параметром типа dynamic. В точке
резолвинга передаёте params: с нужным значением.

Цена: params — dynamic. Приводите к типу сразу внутри биндинга, не снаружи.

И осторожно с .singleton() на параметрическом биндинге: закешируется объект
с первым переданным параметром.

---

### Р3 · Асинхронные зависимости · ~1,5 мин

Некоторые объекты требуют async-инициализации: база данных, чтение из хранилища,
сетевой запрос при старте.

Для этого — пара toProvideAsync и resolveAsync. Важно: они строго парные.
Синхронный resolve на async-биндинге бросит исключение.

toProvide принимает FutureOr<T>. toProvideAsync — строго Future<T>. Для
асинхронных зависимостей всегда используйте toProvideAsync.

Поведение dev.4: если async-синглтон упал при инициализации, следующий
resolveAsync попробует создать его заново.

---

### Р4 · Когда зависимости может не быть · ~1 мин

Два метода резолвинга.

resolve бросает StateError, если биндинга нет. Для обязательных зависимостей
это правильно: падать надо громко, не тихо.

tryResolve возвращает null — но только если биндинг не зарегистрирован.
Незавершённый биндинг — это всё равно StateError, не null.

Правило для команды: tryResolve только там, где отсутствие зависимости —
штатный сценарий. Аналитика, дебаг-инструменты, фичи за флагом.
Всё остальное — resolve.

---

### Р5 · Кодогенерация · ~2 мин

Если устали писать bind<T>().toProvide(...) вручную — есть кодогенератор.

Аннотируете класс модуля как @module(), методы как @provide() и @singleton().
Аргументы метода превращаются в currentScope.resolve<...>() в сгенерированном коде.

Запускаете build_runner — получаете класс $AppModule, который использует
тот же механизм bind<T>(), только написанный машиной.

Три обязательных условия: part-директива в файле, abstract class, наследование
от Module. Забыли part — генератор молчит без ошибки.

Когда включать: модули с двадцатью и более биндингами. Меньше — пишите руками,
это прозрачнее.

---

### Р6 · Циклические зависимости · ~1,5 мин

По умолчанию цикл зависимостей приводит к переполнению стека. Вместо этого
можно получить исключение с цепочкой A → B → A.

Два уровня детекции: локальный — цикл внутри скоупа, кросс-скоуп — через
границы скоупов.

Включайте в debug-сборке и тестах. В проде стоит времени на каждом резолве.

Внимание на номенклатуру: на уровне CherryPick enableGlobalCycleDetection —
это локальный для всех скоупов. А enableGlobalCrossScopeCycleDetection —
кросс-скоуп для всех. Названия контринтуитивны исторически — запомните один раз.

---

### Р7 · Наблюдатель · ~1,5 мин

14 событий: создание объекта, запрос, кеш, установка и снятие модуля,
открытие и закрытие скоупа, цикл, ошибка.

Реализуйте CherryPickObserver — только implements, не extends. Наследование
от SilentCherryPickObserver намеренно отключает все колбэки через fast-path
в Scope.

Готовая интеграция: TalkerCherryPickObserver из пакета talker_cherrypick_logger.
Одна строка — и все события в вашем логгере.

Практически: считайте объекты в каждом скоупе после логина и после логаута.
Если что-то осталось — вот где смотреть.

---

### Р8 · Кто закрывает скоуп экрана · ~2 мин

Два рабочих подхода.

Первый: State.dispose. В initState открываете скоуп, в dispose закрываете.
Просто, понятно, работает для одноэкранных сценариев.

Второй: guard роутера. Guard открывает скоуп на входе в поток — первый экран
оплаты, первый шаг подписания. Guard закрывает на выходе — последний экран
или отмена. Все промежуточные экраны получают тот же скоуп по имени.

Для процессных скоупов, которые переживают пересоздание экрана — роутер.
Guard знает, когда поток начался и когда закончился. Экран этого не знает.
| **Итого** | | **~14 мин** |
